我们需要在本地搭建一个可持久化、鲁棒性高、可恢复、可回退的一套本机AI协作工作流。

搭建这样一个工作流主要有三点考虑。

1.第一点考虑是不同模型的能力不同，不同厂商的约束不同，功能也不同。我们不能寄希望于完全使用于厂商的工具，而是应该把它当做一个独立的能力单元去完成任务。
2.第二点是现在AI工具众多，我们需要一个详细的工作流程把它串联起来，而不是由人在其中必须要发挥监视和点击继续的作用。

核心想法如下，我们可能会通过一个langgraph来串联起来一些工作流，比如说CodeX、OpenCode、Cloud、Harmess Agent之类的主流的Agent框架。

我们至少需要以下的组件，第一个是Adapter，Adapter主要用于解决具体Agent工具调用远程API不一致的问题。比如说大部分情况，我们可能不是使用官方的订阅，而是使用中转， Base URL加APIKEY的形式，或者说使用公司内部的提供的URL和KEY。这点需要借鉴一下New API和Sub2API的一些设计思路。 把复杂性在这一层消除掉。

第二个组件是监控层，需要实时监控流量从哪里流出，调用了多少Token，缓存命中率如何，实际消耗是怎么样的？是否是执行回退，还是正在在工作？

第三个部分是我们核心的业务连接层，我可以简单的分为开发、修复、重构、维护、上线五个阶段。

在开发的阶段，一个简单的想法是，我可以用一个比较强大的模型做调研，然后由具体的模型去执行，再有第三个模型去审查。那么执行的模型往往比调研的模型能力要弱，而审查的模型往往是最最强大的模型。比如说我可以用Gemini三点一Pro进行调研，然后将上下文传递给Cloud OPS或者Cloud Sonnet进行执行，最后我由GPT五进行审核。这样可以结合一下Loop Engineering的一些想法, 目的是在有效的空间和时间中充分压榨AI的能力，并且把它转化成实际的生产力。

在修复bug和维护、上线、重构等阶段，也需要同样的思路和逻辑，这个你可以帮我构建。

第四个部分是我们需要一个可视化监控层。首先我需要实时看到当前在哪一步运转，具体的哪一个Agent在干什么事情，并且我还能够看到它所输出的一些上下文，来判断它的一些问题，防止它陷入死循环。这样的可视化图必须先进、美观，并且实时更新，保证状态同步，而不是使用乐观更新等一些trick的手法。


第五个部分，我们需要一个核心控制层来控制什么时候停止，什么时候开始。可能会有定时的任务，可能会有停止的条件。这方面要充分考虑人的需求和业务逻辑。

对于技术实践，我们坚持要使用稳定的技术栈、主流的技术栈，而不是上来就用Python等弱类型语言。这种语言不易于维护，也很容易出bug。在前端方面，我们必须要使用大量现成的库。

对于后端而言，我的想法是Rust、TS或者说C# 都是可以接受的选项。
对于前端而言，React和Vue是可以接受的选项。对于CSS而言，必须使用Tailwind的CSS。对于框架而言，可以使用现成的框架，也应该使用现成的框架， 必须选一个可以和Tailwind CSS搭配的框架，最好是无头框架。

对于UI设计方面，我们必须保证UI设计的响应式，使用最佳的响应式设计逻辑。
在代码规范方面，必须使用各个语言的linter和代码质量分析系统进行分析。必须按照最佳实践和命名规范。必须满足单一职责原则，不允许单个文件过长。必须保证组件可复用、可维护。
